# Theme to use for the website
theme = "terminimal"

# The URL the site will be built for
base_url = "https://jujpenabe.github.io"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# Put all your custom variables here
accent_color = "blue-auto"

background_color = "auto"

# The logo text - defaults to "Terminimal theme"
logo_text = "Web 3D Gaussian Splatting"

# Author name - defaults to "terminimal"
# author = "Juan José Peña Becerra"

copyright_html = " copy; 2025 Juan José Peña Becerra <b>Universidad Nacional de Colombia</b>"

# menu is enabled by adding menu_items
menu_items = [
  {name = "blog", url = "$BASE_URL"},
  {name = "archive", url = "$BASE_URL/archive"},
  {name = "about me", url = "$BASE_URL/about"},

]
